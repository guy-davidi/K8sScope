/* style.css */

/* Body tweak: slightly off-white background to match the Bootstrap background theme. */
body {
  background-color: #f8f9fa;
}

/* Toast override: ensure success/error toasts stand out. */
.toast.text-bg-success .toast-body {
  background-color: #d4edda; /* Light green background */
  color: #155724;           /* Dark green text */
}
.toast.text-bg-danger .toast-body {
  background-color: #f8d7da; /* Light red background */
  color: #721c24;            /* Dark red text */
}

/* Optional custom gradients for card headers */
.card-header.bg-info {
  background: linear-gradient(135deg, #17a2b8 0%, #138496 100%);
}
.card-header.bg-secondary {
  background: linear-gradient(135deg, #6c757d 0%, #5a6268 100%);
}
.card-header.bg-dark {
  background: linear-gradient(135deg, #343a40 0%, #23272b 100%);
}

/* Example “Execute” button styling to stand out */
#ebpf-form button[type="submit"] {
  font-size: 1.1rem;
  font-weight: 600;
  letter-spacing: 0.5px;
}

/* Misc spacing or typography tweaks */
.form-label.fw-bold {
  font-weight: 600 !important;
  margin-bottom: 0.3rem !important;
}

/* .o file list hover effect */
#o-file-list li:hover {
  background-color: #e2e6ea;
  cursor: pointer;
}
