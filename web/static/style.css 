:root {
  --body-bg: #f8f9fa;
  --toast-success-bg: #d4edda;
  --toast-success-text: #155724;
  --toast-danger-bg: #f8d7da;
  --toast-danger-text: #721c24;
  --card-header-info-gradient: linear-gradient(135deg, #17a2b8 0%, #138496 100%);
  --card-header-secondary-gradient: linear-gradient(135deg, #6c757d 0%, #5a6268 100%);
  --card-header-dark-gradient: linear-gradient(135deg, #343a40 0%, #23272b 100%);
  --o-file-hover-bg: #e2e6ea;
  --transition-speed: 0.25s;
  --navbar-height: 60px;
  --footer-height: 50px;
}

html, body {
  height: 100%;
  margin: 0;
  overflow: hidden;
  background-color: var(--body-bg);
  font-family: "Helvetica Neue", Helvetica, Arial, sans-serif;
}

header, footer {
  flex-shrink: 0;
}

.main-content {
  height: calc(100vh - var(--navbar-height) - var(--footer-height));
  overflow: auto;
  padding-top: 1rem;
  padding-bottom: 1rem;
}

.navbar, .footer {
  height: var(--navbar-height);
  min-height: var(--navbar-height);
}

.list-scroll, .table-scroll, .log-scroll {
  max-height: 40%;
  overflow-y: auto;
}

.card {
  margin-bottom: 0.5rem;
}

.card-header {
  font-weight: 600;
  padding: 0.5rem 1rem;
  font-size: 0.9rem;
}

.card-body {
  padding: 0.5rem 1rem;
  font-size: 0.8rem;
}

.custom-table th, .custom-table td {
  padding: 0.4rem;
  vertical-align: middle;
  font-size: 0.8rem;
}

.custom-table tbody tr:hover {
  background-color: #f1f1f1;
}

#o-file-list li {
  padding: 0.3rem 0.75rem;
  border-bottom: 1px solid #e9ecef;
  font-size: 0.8rem;
  transition: background-color var(--transition-speed) ease;
}

#o-file-list li:hover {
  background-color: var(--o-file-hover-bg);
  cursor: pointer;
}

#o-file-list li:last-child {
  border-bottom: none;
}

#o-file-list li.active {
  background-color: #0d6efd;
  color: white;
}

.form-label.fw-bold {
  font-weight: 600 !important;
  margin-bottom: 0.3rem !important;
}

.form-control, .form-select {
  font-size: 0.8rem;
  padding: 0.375rem 0.5rem;
}

.btn-lg {
  padding: 0.375rem 0.75rem;
  font-size: 0.9rem;
  transition: background-color var(--transition-speed) ease, transform 0.15s ease;
}

#ebpf-form button[type="submit"]:hover {
  transform: scale(1.02);
}

button:disabled {
  opacity: 0.6;
  cursor: not-allowed;
}

#userspaceOutput {
  background-color: #f1f1f1;
  border: 1px solid #ccc;
  padding: 0.5rem;
  font-family: monospace;
  white-space: pre-wrap;
  max-height: 200px;
  overflow-y: auto;
}
